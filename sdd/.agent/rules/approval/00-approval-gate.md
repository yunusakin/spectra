# Approval Gate

- After intake, write spec files under `sdd/memory-bank/`.
- Stop and ask for explicit approval (reply "approved").
- Do not generate any application code before approval.
- When approved, generate code only under `app/`.
