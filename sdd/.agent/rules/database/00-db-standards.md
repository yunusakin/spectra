# Database Standards

- Use migrations for schema changes.
- Add indexes for frequently filtered columns.
- Use transactions where consistency is required.
- Keep naming consistent (table/column conventions).
