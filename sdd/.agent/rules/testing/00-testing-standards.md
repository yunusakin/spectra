# Testing Standards

- Unit tests required for core logic.
- Integration tests for data access and external boundaries.
- Keep tests deterministic and fast.
- Define coverage targets in project docs.
