{
  "name": "spectra-workspace",
  "private": true,
  "version": "3.1.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "spectra": "node packages/cli/bin/spectra.js",
    "doctor": "node packages/cli/bin/spectra.js doctor",
    "check": "bash scripts/validate-repo.sh --strict",
    "verify": "bash scripts/verify-work.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
